server:
  servlet:
    context-path: /book-market/api
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/book_market
    username: postgres
    password:
